Basic add-on to Uptime Kuma that pulls data from kuma's sqlite3 db and processes it into monthly averages for each monitor, then displays it in a simple html/js web page that allows csv download.

Must set a cron job and ensure the relative paths in the bash script are correct.

